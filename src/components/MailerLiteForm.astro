---
// No props — drop this component wherever you want the signup form
---

<section class="bg-base-200 border-y border-base-300">
  <div class="max-w-2xl mx-auto px-4 lg:px-8 py-12 sm:py-16 text-center">
    <h2 class="text-2xl sm:text-3xl font-bold tracking-tight" style="font-family: 'Outfit', sans-serif;">
      Never Miss a New Program
    </h2>
    <p class="text-base-content/55 mt-2 mb-6 leading-relaxed">
      A new 4-week program drops every month. Get it delivered straight to your inbox — no spam, just training.
    </p>
    <div class="ml-embedded" data-form="NcrATH"></div>
  </div>
</section>

<style is:global>
  /* ── MailerLite submit button → site primary color ── */
  .ml-form-embedSubmit button,
  .ml-embedded button[type="submit"],
  .ml-embedded .primary {
    background-color: oklch(0.65 0.2 25) !important;
    border-color: oklch(0.65 0.2 25) !important;
    font-family: 'Outfit', sans-serif !important;
    font-weight: 600 !important;
    letter-spacing: 0.01em !important;
    border-radius: 0.5rem !important;
    transition: background-color 0.15s ease, border-color 0.15s ease !important;
  }

  .ml-form-embedSubmit button:hover,
  .ml-embedded button[type="submit"]:hover,
  .ml-embedded .primary:hover {
    background-color: oklch(0.57 0.2 25) !important;
    border-color: oklch(0.57 0.2 25) !important;
  }

  /* ── Input fields ── */
  .ml-embedded .ml-field-group input,
  .ml-form-fieldRow input {
    font-family: 'DM Sans', sans-serif !important;
    border-radius: 0.5rem !important;
  }

  /* ── Dark theme adjustments ── */
  [data-theme="gymdark"] .ml-form-embedSubmit button,
  [data-theme="gymdark"] .ml-embedded button[type="submit"],
  [data-theme="gymdark"] .ml-embedded .primary {
    background-color: oklch(0.7 0.2 25) !important;
    border-color: oklch(0.7 0.2 25) !important;
  }

  [data-theme="gymdark"] .ml-embedded .ml-field-group input,
  [data-theme="gymdark"] .ml-form-fieldRow input {
    background-color: oklch(0.22 0.01 260) !important;
    color: oklch(0.9 0.005 80) !important;
    border-color: oklch(0.28 0.01 260) !important;
  }
</style>
